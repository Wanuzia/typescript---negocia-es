//uma interface não pode ser instanciada, não é possível criar new a partir dele, não possui constructor.
//inmterface não é classe não pode ser instanciada
//não podemos usar os modificadores private, protected e public nas propriedade de uma interface
export interface NegociacoesDoDia {
    montante: number;
    vezes:number;
}